# frontend-architecture

An installable agent skill: tech-stack-agnostic patterns for structuring a component-based frontend app — component tiering, feature-folder organization, data/service boundaries, state-management layering, page composition & data loading, and forms/schema validation.

Planning is underway on this repo's wayfinder map (issue #1) before the skill's actual `SKILL.md` is written.
